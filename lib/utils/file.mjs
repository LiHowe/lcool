import fs from 'fs'
import path from 'path'

/**
 *
 * @param path {String}
 * @param onSuccess {Function}
 * @param onError {Function}
 * @param createFolder {Boolean}
 */
export function writeFile({
  path,
  onSuccess,
  onError,
  createFolder,
}) {
}
