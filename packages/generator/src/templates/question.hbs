/**
* @lcool {{ createTime }}
* 序号: {{ questionId }}
* 名称: {{ translatedTitle }} | {{ title }}
* 难度: {{ difficulty }}
* 标签: {{#each tags}} {{this}} {{#if @last}}{{else}}|{{~/if}}{{/each}}
* 链接: {{ link }}
* -------------------------------------------------
*
{{#each detail}}
* {{{this}}}
{{/each}}
*
* -------------------------------------------------
*
*/

const { test } = require('./utils')
// @lcool code-start
{{{code}}}
// @lcool code-end
test()
